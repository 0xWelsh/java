# JPanel
We can use JPanel class to add multiple components to a window.
JPanel is a component that holds other components. To use a JPanel, you'd
follow this basic flow:
- create an instance of JPanel
- add components to the JPanel instance
- add that JPanel to the JFrame


